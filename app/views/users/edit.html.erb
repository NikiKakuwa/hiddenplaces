<div class="post post-form">
<%= form_for(@user) do |f| %>
<% if @user.errors.any? %>
    <h3>
    <% @user.errors.count %>errors prohibited this book from being saved:
    </h3>

    <ul>
    <% @user.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
    </ul>
    <% end %>
        ユーザー名<br>
        <%= f.text_field :name, autofocus: true, class:"form" %><br><br>
        プロフィール画像<br>
        <%= attachment_image_tag @user, :user_image, fallback: "no_image.jpg", class:"user-profile-image" %>
        <br><br>
        新しいプロフィール画像<br>
        <%= f.attachment_field :user_image %><br>
        自己紹介<br>
        <%= f.text_area :introduction, class:"form" %><br><br>
    <%= f.submit "新規登録", class:"btn-sm btn-success" %>
<% end %>

</div>