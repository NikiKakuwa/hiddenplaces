<h2>Post detail</h2>
<table class="table table-hover table-inverse">
    <thead>
        <tr>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><%= link_to user_path(@post.user.id) do %>
                <%= attachment_image_tag @post.user, :user_image, :fill, 40, 40, fallback: "no_image.jpg", size:'40x40' %>
                <% end %>
                <%= link_to @post.user.name, user_path(@post.user) %>
            </td>
            <td>
            <%= attachment_image_tag @post, :post_image, :fill, fallback: "no_image.jpg", size:'150x150' %>
            <%= link_to @post.title, post_path(@post) %>
            <%= @post.body %>
            <% if @post.user_id == current_user.id %>
            <%= link_to "Edit", edit_post_path(@post) ,class: "btn-sm btn-success" %>
            <%= link_to "Destroy", post_path(@post), method: :delete, data: { confirm: 'Are you sure?' } , class: "btn-sm btn-danger"%>
            <% end %>
            </td>
            <%# <td> %>
            <%# <% if @post.favorited_by?(current_user) %>
                <%# <%= link_to book_favorites_path(@post), method: :delete do %>
                <%# <i class="fa fas-heart" aria-hidden="true" style="color: red;"></i> %>
                <%# <%= fa_icon "heart", :style=>"color:red;" %>
                <%# <% end %>
            <%# <% else %>
                <%# <%= link_to book_favorites_path(@post), method: :post do %>
                <%# <i class="fa far-heart></i> %>
                <%# <%= fa_icon "heart-o", :style=>"color:red;" %>
                <%# <% end %>
            <%# <% end %>
             <%# <h6><%= @post.favorites.count %>
            <%# </td> %>
        </tr>
    </tbody>
</table>